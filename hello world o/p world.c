#include<stdio.h>
#include<conio.h>
void main()
{
    char a[200],b[200];
    int i,j,n1,n2;
    scanf("%s%s",&a[i],&b[i]);
    n1=strlen(a);
    n2=strlen(b);
    if(n1<n2)
    {
        printf("%s",a);
    }
    else
    {
        printf("%s",b);
    }
}
