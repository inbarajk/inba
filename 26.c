#include<stdio.h>
#include<conio.h>
void main()
{
    int a,b = 1;

    for(a=2; a <= n/2; ++a)
    {
        if (n%a == 0)
        {
            b =0;
            break;
        }
    }
    return flag;
}
{
    int a, c, i,b;
    printf(" two positive integers: ");
    scanf("%d %d", &a, &c);
    printf("Prime numbers  %d and %d : ", a, c);
    for(i=n1+1; i<n2; ++i)
    {
        if(flag == 1)
            printf("%d ",i);
            }
            }
